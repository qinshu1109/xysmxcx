# 校园拾喵驿站交付启动说明

这份说明给第一次打开项目的人看。按这里做，就可以在 Windows 电脑上把系统跑起来。

## 1. 这个系统是做什么的

校园拾喵驿站是一套本地演示系统，用来管理校园流浪猫信息、求助领养信息、评论、轮播图和基础配置。

系统分为三部分：

- 微信小程序：给同学使用，可以看猫咪、看求助领养、登录、发布、评论、查看自己的内容。
- 管理后台：给管理员使用，可以管理猫咪、求助领养、评论、用户、轮播图和基础配置。
- 后端和数据库：负责保存真实数据，小程序和后台都从这里读取数据。

本项目是本地作业演示版本，不需要上线服务器，不需要 Docker，不需要 Linux。

## 2. 客户电脑需要先准备什么

请先安装：

- Windows 10 或 Windows 11。
- Node.js 18 或更高版本。
- MySQL 8.x Windows 版。
- 微信开发者工具。

说明：Node.js 不是 Docker。它是 Windows 上的程序运行环境，作用类似 Java JDK。本项目的后端接口和后台管理系统是用 Node.js 启动的，所以电脑需要 Node.js；MySQL 只负责存数据，不能替代 Node.js。

建议 MySQL 安装时记住 root 密码。为了少出错，可以设置为：

```text
123456
```

如果已经安装过 MySQL，用原来的密码也可以。

## 3. 最快启动方式

解压压缩包后，进入项目文件夹，双击：

```text
一键启动.bat
```

这个脚本会自动做这些事：

- 检查电脑有没有 Node.js、npm 和 MySQL 命令。
- 如果没有 Node.js，会直接拉起包内 `runtime/nodejs/` 下的 Windows Node.js 安装包，不需要现场下载。
- 尝试启动 MySQL 服务。
- 尝试连接 MySQL。
- 第一次运行时创建数据库并导入测试数据。
- 自动安装后端和后台依赖。
- 自动启动后端接口。
- 自动启动后台管理系统。
- 自动打开后台地址。

启动成功后会打开后台：

```text
http://localhost:5173
```

注意：脚本启动后会出现后端和后台两个黑色命令行窗口，演示期间不要关闭它们。关掉窗口，系统服务就停了。

## 4. 登录账号

后台管理员账号：

```text
账号：admin
密码：123456
```

小程序普通用户账号：

```text
账号：cat_user01
密码：123456
```

小程序管理员用户账号：

```text
账号：cat_admin01
密码：123456
```

说明：登录注册采用账号和密码模拟登录，不接入真实手机号、短信验证码和微信授权。

## 5. 打开微信小程序

启动后端以后，再打开微信开发者工具。

操作步骤：

1. 打开微信开发者工具。
2. 选择“导入项目”。
3. 选择解压后的项目文件夹。
4. 确认小程序源码目录是 `miniprogram/`。
5. 编译运行。

小程序默认连接本机后端：

```text
http://localhost:3000/api
```

如果后端没启动，小程序会加载失败或登录失败。

## 6. 推荐演示顺序

建议按这个顺序演示，最容易看出前后台已经联动：

1. 先双击 `一键启动.bat` 启动系统。
2. 打开后台，用 `admin / 123456` 登录。
3. 在后台查看猫咪管理、求助领养管理、评论管理、用户管理。
4. 用微信开发者工具打开小程序。
5. 小程序用 `cat_user01 / 123456` 登录。
6. 在小程序发布一条求助或领养信息。
7. 回到后台刷新求助领养管理，能看到刚才发布的数据。
8. 在后台修改求助状态。
9. 回到小程序刷新详情页，能看到状态变化。
10. 在小程序发表评论。
11. 回到后台评论管理，能看到评论。
12. 后台删除评论后，小程序刷新评论列表，评论会消失。

这条链路能说明：小程序、后台、后端、数据库是连在一起的，不是前端假数据。

## 7. 如果需要重置测试数据

如果数据被改乱了，想回到初始演示数据，执行：

```text
一键启动.bat --reset
```

注意：这个操作会重新导入测试数据，之前演示时新增的数据可能会被清掉。

## 8. 如果一键启动失败怎么办

### 提示 node 或 npm 找不到

说明电脑没有装 Node.js，或者安装时没有加入系统 PATH。

Node.js 是本项目后端和后台的运行环境，不是 Docker。客户电脑只要安装 Windows 版 Node.js 即可，不需要 Linux，也不需要 Docker。

处理办法：

1. 如果脚本提示自动安装，输入 `Y`。
2. 如果自动安装失败，手动安装 Node.js LTS Windows 版。
3. 安装时勾选加入 PATH。
4. 重新打开命令行或重启电脑。
5. 再双击 `一键启动.bat`。

### 提示 mysql 找不到

说明电脑没有装 MySQL，或者 MySQL 的 bin 目录没有加入 PATH。

处理办法：

1. 确认已安装 MySQL 8.x Windows 版。
2. 把 MySQL 安装目录下的 `bin` 加入系统 PATH。
3. 重新打开命令行或重启电脑。
4. 再双击 `一键启动.bat`。

### 提示 MySQL 密码错误

脚本会先尝试 root 空密码和 root / 123456。

如果都不对，会提示输入 MySQL root 密码。输入安装 MySQL 时设置的密码即可。

也可以这样启动：

```text
一键启动.bat 你的MySQL密码
```

### 后台提示 Network Error

通常是后端没有启动，或者后端连不上数据库。

处理办法：

1. 确认后端命令行窗口没有报错退出。
2. 打开浏览器访问 `http://localhost:3000/api/health`。
3. 如果打不开，重新运行 `一键启动.bat`。
4. 如果仍然失败，检查 `server/.env` 里的 MySQL 密码是否正确。

### 中文变成乱码

不要用普通命令手动导入 SQL。

请使用项目自带脚本重新导入：

```text
database\import-win.bat
```

或者运行：

```text
一键启动.bat --reset
```

项目脚本已经处理了中文字符集。

### 端口被占用

后端默认端口是 3000，后台默认端口是 5173。

如果提示端口被占用，通常是之前打开的系统窗口没关干净。

处理办法：

1. 关闭之前打开的后端和后台命令行窗口。
2. 重新双击 `一键启动.bat`。
3. 如果还不行，重启电脑后再启动。

### npm install 很慢或失败

依赖需要从 npm 下载，第一次运行会比较慢。

如果一直失败，可以在命令行执行：

```text
npm config set registry https://registry.npmmirror.com
```

然后重新双击 `一键启动.bat`。

## 9. 手动启动方式

如果不想用一键脚本，可以按顺序手动启动。

第一步，导入数据库：

```text
database\import-win.bat
```

第二步，启动后端：

```text
server\start-win.bat
```

第三步，启动后台：

```text
admin\start-win.bat
```

第四步，用微信开发者工具导入项目并运行小程序。

## 10. 交付内容说明

压缩包内主要包含：

- `miniprogram/`：微信小程序源码。
- `server/`：后端接口服务。
- `admin/`：后台管理系统。
- `database/`：数据库建表和测试数据。
- `docs/`：需求、接口、数据库和验收文档。
- `一键启动.bat`：Windows 一键启动脚本。
- `runtime/nodejs/`：随包附带的 Windows 版 Node.js 安装包；客户电脑没有 Node.js 时，脚本会直接打开这个本地安装包，不需要现场下载。
- `README.md`：开发版运行说明。
- `交付启动说明.md`：客户版启动说明。

压缩包不包含：

- `node_modules/` 依赖目录。
- 本机 `.env` 私有配置。
- 运行日志。
- 上传测试文件。
- 原始素材包和效果图大文件。

第一次启动时脚本会自动安装需要的依赖。

## 11. 不包含哪些功能

本地作业演示版本不包含：

- 真实微信授权。
- 真实短信验证码。
- 真实手机号登录。
- 地图定位。
- 在线支付。
- 线上部署。
- Docker 或 Linux 环境。

本项目需要：

- Windows 版 Node.js：启动后端接口和后台管理系统。
- Windows 版 MySQL：保存业务数据。
- 微信开发者工具：运行小程序。

这些不是本版本的验收范围。
