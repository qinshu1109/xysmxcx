## 首页像素复刻 checkpoint

已确认：

- 用户确认真实小程序路径为 `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\app.json`。
- 本次只实现首页，不实现后端、后台、数据库和其他业务页面。
- 首页专属素材来源为已解压的 `首页专属裁剪素材包\static\home`。

未确认：

- 未在微信开发者工具中执行视觉预览。
- 未在真机中验证安全区与底部栏。

已执行：

- 已复制首页专属素材到 `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\static\home`。
- 已将小程序入口收敛为 `pages/home/index`。
- 已创建原生小程序首页文件与锁定 mock 数据。
- 已移除默认 TypeScript/LESS 模板页面入口。

未执行：

- 未安装 npm 包。
- 未创建后端、后台、数据库代码。
- 未创建猫咪图鉴、求助领养、我的页面。
- 未提交、未推送。

改动文件：

- `C:\Users\Administrator\WeChatProjects\miniprogram-10\project.config.json`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\app.json`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\app.js`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\app.wxss`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\sitemap.json`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\utils\mockData.js`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.js`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.json`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.wxml`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.wxss`

验证结果：

- 已通过：首页专属素材复制完成。
- 未执行：微信开发者工具预览。
- 未执行：真机验收。

阻塞项：

- 无。

下一步：

- 使用微信开发者工具打开 `C:\Users\Administrator\WeChatProjects\miniprogram-10`，预览首页并根据截图微调间距。

禁止继续做：

- 不创建其他业务页面。
- 不接后端。
- 不改 Obsidian 原素材目录。

## 首页快捷入口与底部栏对齐微调 checkpoint

已确认：

- 用户反馈快捷入口文字与图标、底部栏文字与图标没有对齐。
- 本次只调整首页样式文件，不改素材、不改数据、不创建其他页面。

未确认：

- 未在微信开发者工具中截图复核最终视觉。

已执行：

- 已将快捷入口卡片内部布局改为固定图标列和文字列。
- 已压缩快捷入口标题/副标题字号和行高，避免长文案顶到图标或溢出。
- 已调整底部栏高度、顶部内距、图标尺寸和文字居中。

未执行：

- 未改首页 mock 数据。
- 未改任何图片素材。
- 未提交、未推送。

改动文件：

- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.wxss`

验证结果：

- 已通过：`app.json` 合法，页面文件齐全。
- 已通过：首页图片引用全部存在且全部来自 `/static/home/`。
- 已通过：禁用文案、旧素材路径、Obsidian 绝对路径未命中。
- 未执行：微信开发者工具视觉预览。

阻塞项：

- 无。

下一步：

- 在微信开发者工具中查看首页，若仍有偏差，继续按截图微调 rpx 间距。

## 首页底部栏文字基线微调 checkpoint

已确认：

- 用户反馈底部栏 `首页` 和 `我的` 文字仍未与其他文字对齐。

未确认：

- 未在微信开发者工具中截图复核最终视觉。

已执行：

- 已给每个底部 tab 增加固定图标槽。
- 已将底部 tab 文字改为绝对定位，固定到同一条 `top: 59rpx` 基线。
- 已保留 tab 图标继续引用 `/static/home/` 裁剪素材。

未执行：

- 未改首页数据。
- 未改图片素材。
- 未创建其他页面。

改动文件：

- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.wxml`
- `C:\Users\Administrator\WeChatProjects\miniprogram-10\miniprogram\pages\home\index.wxss`

验证结果：

- 已通过：`app.json` 合法，页面文件齐全。
- 已通过：首页图片引用全部存在且全部来自 `/static/home/`。
- 已通过：禁用文案、旧素材路径、Obsidian 绝对路径未命中。

阻塞项：

- 无。

下一步：

- 在微信开发者工具中刷新首页，确认四个底部文字是否在同一水平线。
