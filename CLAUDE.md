# AGENTS.md

本文件是当前项目的项目补充规则。

全局规则仍以已注入的全局 AGENTS 为最高安全闸门；本文件只补充本项目的开发约束。

## 项目定位

- 本项目是“校园拾喵驿站 / 校园流浪猫驿站系统”的本地大作业实现。
- 系统由三部分组成：微信小程序同学端、Node.js 后端 API、Vue 后台管理系统。
- 默认技术栈：微信原生小程序 + Node.js Express + MySQL + Vue 3 + Element Plus。
- 本项目不做真实微信授权、短信验证码、地图定位、在线支付、线上部署。

## 目录边界

- `miniprogram/`：微信小程序同学端。
- `server/`：后端 API 服务。
- `admin/`：管理员网页后台。
- `database/`：数据库建表与测试数据脚本。
- `docs/`：开发规格、接口契约、页面规格、验收清单。
- `素材/`、`效果图/`：原始资料目录，默认只读，不得修改。
- `校园拾喵驿站_素材处理版/`：处理后的素材来源，默认只读。

## 实现原则

1. 不允许把业务数据写死在前端或后端代码中。
2. 用户、管理员、猫咪、求助领养、评论、轮播图、基础配置都必须来自数据库。
3. 小程序和后台管理系统所有增删改查必须通过后端 API。
4. 数据库结构变化时，必须同步更新 `database/init.sql` 和 `docs/03-数据库设计.md`。
5. 接口变化时，必须同步更新 `docs/04-接口契约.md`。
6. 小程序页面行为变化时，必须同步更新 `docs/05-小程序页面规格.md`。
7. 后台页面行为变化时，必须同步更新 `docs/06-后台管理规格.md`。
8. 素材接入变化时，必须同步更新 `docs/07-素材接入规范.md`。
9. 小程序页面实现、视觉复刻或素材接入前，必须先读取 `docs/11-小程序前端一次性完成度门禁.md`。
10. 每完成一个开发阶段，必须更新 `_操作日志.md`，区分已通过、已失败、未执行、当前环境无法验证。
11. 不提交、不推送，除非用户明确要求。

## 素材规则

- 不修改 `素材/`、`效果图/`、`校园拾喵驿站_素材处理版/` 中的原始文件。
- 小程序运行所需素材应复制到 `miniprogram/static/`。
- 正式图标优先使用 `校园拾喵驿站_素材处理版/icons/svg/`。
- Banner、背景、猫咪照片优先使用 `校园拾喵驿站_素材处理版/png/opaque/` 下的 WebP 或 PNG。
- 透明装饰和吉祥物优先使用 `校园拾喵驿站_素材处理版/png/transparent_fixed/`。
- 数据库 seed 中的图片路径必须和小程序静态路径一致。

## 开发顺序

1. 先完成 `docs/` 工程规格。
2. 再完成 `database/init.sql` 和 `database/seed.sql`。
3. 再实现 `server/` 后端 API。
4. 再实现 `admin/` 后台管理。
5. 再实现 `miniprogram/` 小程序页面。
6. 最后按 `docs/09-测试验收清单.md` 联调验收。

## 禁止事项

- 禁止一上来实现整个系统。
- 禁止跳过数据库和接口契约直接写页面。
- 禁止用前端假数据冒充数据库数据。
- 禁止为了界面演示绕过后端 API。
- 禁止跳过 `docs/11-小程序前端一次性完成度门禁.md` 直接按效果图或素材猜页面。
- 禁止修改原始素材目录。
- 禁止创建真实生产配置、真实密钥、真实线上部署脚本。
- 禁止把 `.env`、数据库 dump、账号密码、token、secret 提交或写入公开文档。

## 小程序页面复刻与素材源规则

本节是页面视觉复刻、素材接入、截图还原、裁剪图使用的强制规则。若本节与其他规则冲突，执行更严格者。

### 先读门禁

- 所有小程序页面实现、视觉复刻、素材接入、截图还原、裁剪图使用前，必须先读取 `docs/11-小程序前端一次性完成度门禁.md`。
- 读取后必须明确页面边界、可写范围、禁止修改范围、参考来源、素材来源、验收口径。

### 页面级素材源优先级

页面级复刻时，素材源优先级必须是：

1. 当前页面专属已解压素材包。
2. 当前页面专属素材包中的 `manifest`、`README`、`pixel spec`。
3. 当前页面专属素材包压缩包。
4. 用户当前明确指定的截图或效果图。
5. 通用处理版素材。
6. 原始效果图 fallback。

只有当前一层明确不存在、无法读取，或用户明确批准时，才允许进入下一层。不得在未检查页面专属素材包的情况下直接使用效果图 fallback。

### 路径相对化

- 项目文档、规则、门禁、复盘和最终报告中不得新增本机绝对路径。
- 用户提供的本机路径只能作为外部上下文；写入规则时必须转译成“当前工作区根”“小程序源码根”“页面专属素材包”“`docs/`”“`static/home/`”等路径角色。
- 运行时图片路径不得引用资料目录，只能使用小程序内运行路径或接口返回路径。

### 先发现，再执行

页面复刻前必须先列出：

- 当前工作区根。
- 小程序源码根。
- 目标页面路径。
- 权威素材包。
- 素材包内文件清单。
- 可写入范围。
- 禁止修改范围。
- 参考图或截图来源。

没有完成发现，不允许写页面代码、裁剪图片、复制素材或生成图片。

### 裁剪前置校验

任何裁剪动作前必须：

- 确认源图来自权威素材源。
- 记录源图相对路径。
- 记录源图实际尺寸。
- 记录裁剪区域来源。
- 输出裁剪尺寸或预览结论。
- 源图尺寸与裁剪参数不匹配时立即停止。
- 裁剪框来自假设而不是标注时立即停止并要求确认。
- 禁止直接对效果图 fallback 裁剪，除非用户明确批准。

### 失败升级机制

同一页面同一视觉区域连续两轮 WXML/WXSS 调整仍不接近参考图时，必须停止局部调整：

- 停止继续调 `margin`、`font-size`、`transform`。
- 复盘差异类型。
- 判断是否需要模块级素材或重新裁剪。
- 若使用模块级素材，必须从权威素材源生成或裁剪。
- 必须写入页面几何记录和素材映射。

### Stop-the-line 停止条件

出现以下任一情况必须停止：

- 找不到页面专属素材包。
- 找不到任务依赖的素材包 `manifest`、`README` 或 `pixel spec`。
- 发现多个候选素材源，无法确定权威来源。
- 源图尺寸与裁剪参数不匹配。
- 需要 fallback 到效果图但用户没有授权。
- 需要写入本机绝对路径。
- 需要修改超出本轮允许范围的文件。
- 视觉验收必须依赖微信开发者工具截图，但当前没有执行截图验证。

### 交付诚实性

最终回复必须区分：

- 已完成。
- 已验证。
- 未验证。
- 未执行。
- 当前环境无法确认。
- 仍有风险。
- 下一步。

禁止把静态检查写成视觉通过。禁止把 mock 成功写成后端联调通过。禁止把生成文件写成微信开发者工具预览通过。
